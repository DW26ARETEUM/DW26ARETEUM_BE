spring:
  application:
    name: daedongje

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

app:
  image-base-url: ${IMAGE_BASE_URL:http://localhost:8080}